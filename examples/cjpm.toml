[package]
cjc-version = "1.0.5"
name = "examples"
version = "0.1.0"
output-type = "executable"
description = "Gallery of plot examples: one builder per chart type, viewable or exportable."

[dependencies]
plot = { path = ".." }
